{
    "$schema": "http://json-schema.org/draft-04/schema",
    "title": "DatabaseCollection",
    "oneOf": [
        {"type": "string", "pattern": "^[a-z_]+$"},
        {
            "type": "object",
            "properties": {
                "name": {"type": "string", "pattern": "^[a-z_]+$"},
                "namespace": {"type": "string", "pattern": "^[a-z_]+$"}
            },
            "required": ["name", "namespace"]
        }
    ]
}